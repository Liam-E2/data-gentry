2. Insert + return full row (sometimes useful)
stmt = (
    insert(Embeddings)
    .values(vector=vec)
    .returning(Embeddings)
)

with engine.begin() as conn:
    row = conn.execute(stmt).one()

new_id = row.id

3. ORM-style Se